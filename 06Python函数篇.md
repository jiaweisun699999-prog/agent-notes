> 📌 **[AI 大模型与云原生全栈知识库](./README.md)** / **模块二：Python 编程基础与进阶**
> 🏠 [返回主页 README](./README.md) \| ⚡ [面试 30 分钟速记](./interview/00_面试冲刺30分钟速记卡片.md) \| 🎓 [本模块面试题](./interview/01_云原生与Python高频面试题.md)

---

## Python高级

### 一、函数

#### 1、函数的定义

##### 1.1 开发背景

在编程初期，我们会直接编写顺序执行代码，但随着业务逻辑复杂，会出现大量**重复代码、冗余代码**，同时代码可读性极差、维护成本极高。为解决这一问题，Python引入函数机制：将一段实现特定功能、可重复使用的代码逻辑封装为独立模块，实现**一次定义、多次调用**。

从工程角度，函数是模块化编程的最小单元，核心价值：代码复用、逻辑解耦、便于调试维护、统一业务逻辑。

##### 1.2 标准定义语法

Python通过 `def` 关键字定义函数，属于**动态弱类型**函数定义，无需声明返回值类型、参数类型，语法简洁灵活。

```python
# 基础语法格式
def 函数名(参数列表):
    """函数文档注释（工程必备）"""
    # 函数体：实现具体逻辑的代码
    return 返回值  # 可选
```

##### 1.3 核心定义规则（工程规范）

- 函数名：遵循Python命名规范，小写+下划线，见名知意（禁止拼音、无意义字符）；
- 文档注释：正式项目必须添加，用于说明函数功能、参数含义、返回值、异常场景，方便团队协作；
- 函数体：代码缩进统一为4个空格，单一函数只做一件事（单一职责原则）；
- 空函数：可使用 `pass` 占位，用于功能预留，避免语法报错。

#### 2、函数的参数

参数是函数对外交互的入口，是函数灵活性的核心。Python函数参数分为**位置参数、关键字参数、默认参数、不定长参数**四大类，工程开发中需严格区分优先级和使用场景，避免传参报错。

##### 2.1 位置参数（必选参数）

最基础的参数类型，调用函数时，传入参数的**数量、顺序必须与定义完全一致**，否则直接报错。

```python
def add(x, y):
    return x + y

# 正确：顺序、数量匹配
add(10, 20)
# 错误：数量不匹配、顺序错乱（无语法报错但业务逻辑错误）
```

##### 2.2.关键字参数

调用函数时，通过 `参数名=值` 的形式传参，**无需遵循定义顺序**，仅需保证参数名称正确，大幅提升代码可读性，适合参数较多的场景。

核心规则：关键字参数必须放在位置参数之后，不可混用倒置。

```python
def info(name, age):
    print(name, age)

info(age=18, name="张三")  # 合法
# info(name="李四", 20)  # 非法：位置参数在后
```

##### 2.3 默认参数

定义函数时为参数设置默认值，调用时若未传入该参数，自动使用默认值；传入则覆盖默认值。常用于参数高频固定的业务场景。

**工程核心禁忌**：默认参数必须指向**不可变对象**（int/str/tuple），禁止使用列表、字典等可变对象（下文易错点详细解析）。

```python
def user_info(name, gender="男"):
    print(name, gender)
```

##### 2.4 不定长参数（可变参数）

用于解决参数数量不固定的场景，是工程开发高频用法，分为不定长位置参数、不定长关键字参数。

- ***args 不定长位置参数**：接收任意多个位置参数，最终封装为**元组**，兼容0个、1个、多个参数。
- ***\*kwargs 不定长关键字参数**：接收任意多个关键字参数，最终封装为**字典**。

```python
def flex_func(*args, **kwargs):
    print("位置参数：", args)
    print("关键字参数：", kwargs)

flex_func(1,2,3, name="张三", age=18)
```

##### 2.5 参数优先级（硬性规则）

参数定义顺序：**位置参数 > 默认参数 > \*args > \**kwargs**，违反顺序会直接触发语法错误。

#### 3、函数的返回值

##### 3.1 基础返回值规则

函数通过 `return` 关键字返回执行结果，核心特性：执行到return后，**函数立即终止，后续代码不会执行**。若函数无return语句，默认返回 `None`。

##### 3.2 返回单个值

支持返回任意数据类型（数字、字符串、列表、字典、对象等），是最常用的返回形式。

##### 3.3 返回多个值（Python特性）

Python原生支持函数返回多个值，本质是**自动封装为元组返回**，调用时可直接解包接收，工程中常用于一次性返回多个结果（如计算最大值、最小值、平均值）。

```python
def calc_num(a, b):
    return a+b, a-b, a*b  # 本质：返回元组 (a+b, a-b, a*b)

sum_res, sub_res, mul_res = calc_num(10, 20)  # 多值解包接收
```

##### 3.4 工程使用规范

- 单一功能函数尽量返回单一结果，逻辑清晰；
- 多返回值场景建议控制数量，过多返回值建议封装为字典/对象，提升可读性；
- 禁止无意义return，按需返回结果，避免代码冗余。

### 4、全局变量和局部变量

变量作用域是新手高频踩坑点，核心区别：**定义位置不同，生效范围不同、生命周期不同**。

#### 4.1 局部变量

在函数内部定义的变量，**仅在当前函数内生效**，函数执行结束后自动销毁，无法在函数外部调用。优点：独立隔离，不会污染全局环境。

#### 4.2 全局变量

在函数外部、脚本顶层定义的变量，**整个脚本所有函数均可访问**，生命周期贯穿程序运行全程。

#### 4.3 变量修改核心规则

- 函数内可**直接读取**全局变量，但**无法直接修改**全局变量（会默认创建同名局部变量）；
- 如需在函数内修改全局变量，必须通过 `global` 关键字声明；
- 嵌套函数中修改外层函数变量，使用 `nonlocal` 关键字。

#### 4.4 工程实战建议

**尽量少用全局变量**！全局变量滥用会导致：变量污染、代码耦合度高、调试困难、多线程安全问题。优先使用函数传参+返回值实现数据交互。

### 5、函数中容易出错的

结合十年开发排坑经验，**默认参数使用可变对象**是Python函数最经典、面试最高频、项目最易出bug的问题，针对题目案例完整拆解：

#### 5.1 错误案例还原

```python
# 错误代码：默认参数为可变对象列表
def func(a, lst=[10,20]):
    if a not in lst:
        lst.append(a)  
    print(lst)

# 多次调用测试
func(80)
func(90)
func(70,[10,20])
func(60)
```

#### 5.2 报错与异常结果分析

核心问题：**Python默认参数仅在函数定义时初始化一次，而非每次调用初始化**。

- 函数定义瞬间，创建列表 `[10,20]` 并绑定给默认参数lst；
- 后续无传入lst的调用，会**复用同一个列表对象**，上次调用的修改会保留，造成数据累加错乱；
- 只有手动传入lst参数时，才会使用新列表，打破复用逻辑。

#### 5.3 正确工程写法

将默认参数设为不可变对象None，在函数内部初始化可变对象，保证每次调用都创建新列表。

```python
def func(a, lst=None):
    # 每次调用都重新初始化列表，彻底规避缓存问题
    if lst is None:
        lst = [10,20]
    if a not in lst:
        lst.append(a)
    print(lst)
```

#### 5.4 延伸易错点：多次调用函数的隐性问题

- 可变默认参数数据缓存，导致多次调用结果异常；
- 函数内修改全局变量未声明，出现变量赋值异常；
- return位置错误，导致代码不执行、返回值缺失；
- 传参优先级混乱，位置参数与关键字参数混用报错。

## 6、函数的总结

#### 6.1 核心本质

函数是**代码封装与复用的载体**，所有复杂的Python高级特性（装饰器、生成器、闭包、类方法）均基于基础函数延伸，是Python进阶的核心根基。

#### 6.2 语法核心重点

- 参数是函数的灵魂：熟练掌握五类参数的优先级、使用场景，尤其是不定长参数的灵活运用；
- 返回值灵活适配：支持单值、多值返回，根据业务场景合理封装返回结果；
- 作用域严格区分：杜绝全局变量滥用，理清局部、全局变量的读写规则。

#### 6.3 工程避坑核心准则

- **绝对禁止可变对象作为默认参数**，这是项目中90%函数数据异常的根源；
- 遵循单一职责：一个函数只实现一个核心功能，拒绝大而全的臃肿函数；
- 规范传参和返回值，代码可复用、可调试、可扩展；
- 严格区分变量作用域，减少全局变量，降低代码耦合度。

## 二、函数高级

### 1、递归函数

#### 1.1 递归概念

递归：**函数自己调用自己**的特殊调用形式。核心逻辑是把一个复杂的大问题，拆解为规模更小、逻辑相同的子问题，直到触发终止条件，逐层回溯得到结果。

#### 1.2 递归两大必备条件（工程核心）

- **终止条件**：递归必须有出口，否则会无限递归，触发栈溢出报错 `RecursionError`；
- **递归递进条件**：每次自我调用，问题规模必须缩小，向终止条件靠拢。

#### 1.3 递归执行原理

递归分为两个阶段：**递推阶段**（逐层调用自己，压栈）、**回溯阶段**（触发终止条件后，逐层返回结果，出栈）。Python默认递归深度有限制（默认最大深度约1000），超出限制直接报错。

#### 1.4 实战案例

```python
# 案例：递归计算 n的阶乘 n! = 1*2*3*...*n
def factorial(n):
    # 终止条件
    if n == 1:
        return 1
    # 递归递进：问题规模缩小
    return n * factorial(n - 1)

print(factorial(5))  # 输出：120
```

#### 1.5 递归优缺点与使用场景

- 优点：代码极简、逻辑清晰，适合嵌套拆解类问题；
- 缺点：有栈深度限制、重复计算多、内存开销大，效率低于循环；
- 适用场景：阶乘、斐波那契数列、目录遍历、二叉树遍历、分治算法。

### 2、匿名函数 lambda

#### 2.1 概念与语法

匿名函数：无需通过 `def` 定义、没有函数名的临时函数，专为**简单一次性逻辑**设计，核心作用是精简代码、搭配高阶函数使用。

标准语法：`lambda 形参列表: 表达式`

#### 2.2 核心特性（硬性规则）

- 只能包含**一条表达式**，不能写循环、if语句块（支持三元运算表达式）；
- 表达式的结果会**自动返回**，无需手写 return；
- 无函数名，只能临时调用，或赋值给变量、搭配高阶函数使用。

#### 2.3 基础实战案例

```python
# 1. 无参匿名函数
f1 = lambda: "hello python"
print(f1())

# 2. 单参匿名函数
f2 = lambda x: x ** 2
print(f2(5))  # 输出25

# 3. 多参匿名函数
f3 = lambda x, y: x + y
print(f3(10, 20))  # 输出30

# 4. 搭配三元运算（条件判断）
f4 = lambda x, y: x if x > y else y
print(f4(100, 200))  # 取最大值
```

#### 2.4 工程使用场景

不单独使用，**专门配合高阶函数（map/filter/sorted/reduce）**处理简单逻辑，替代短小def函数，让代码更简洁。

### 3、高阶函数

#### 3.1 高阶函数概念与定义

满足**任意一个**条件的函数即为高阶函数，是函数式编程的核心：

- 接收**另一个函数**作为参数；
- 返回值是**一个函数**。

核心价值：将函数作为“变量”传递，实现逻辑高度抽象、复用，简化批量处理、排序、筛选等通用操作。

### 3.2 普通高阶函数

##### 示例 1：接收函数作为参数（第一种高阶函数形式）

```python
# 定义一个普通功能函数
def calc(x, y):
    return x + y

# 高阶函数：参数可以接收另外一个函数
def run_func(func, a, b):
    # 调用传入进来的函数
    result = func(a, b)
    return result

# 传入calc函数
print(run_func(calc, 10, 20))
```

##### 示例 2：返回值为函数（第二种高阶函数形式）

```python
def outer():
    # 内部定义函数
    def inner():
        print("内部函数执行了")
    # 将函数作为返回值返回
    return inner

# 获取返回的函数对象
f = outer()
# 执行函数
f()
```

##### 示例 3：两种特性结合（传函数参数 + 返回函数）

```python
def create_calc(func):
    def wrapper(a, b):
        print("开始计算")
        return func(a, b)
    return wrapper

def add(x, y):
    return x + y

new_add = create_calc(add)
print(new_add(5, 6))
```

##### 额外补充一个关键区分（新手容易混淆）

```python
# 易错点：函数名 和 函数调用() 的区别
run_func(calc,1,2)   # ✅ 传递函数对象（高阶函数标准写法）
run_func(calc(),1,2) # ❌ calc()代表立刻执行函数，传入的是返回结果，不是函数
```

#### 3.3 Python内置核心高阶函数（全场景详解）

##### 3.3.1 map() 映射函数

功能：对可迭代对象（列表、元组等）中的**每一个元素**，执行指定函数逻辑，返回新的迭代器。

语法：`map(函数, 可迭代对象1, 可迭代对象2...)`

```python
# 案例1：列表元素统一平方
lst = [1, 2, 3, 4, 5]
res = map(lambda x: x ** 2, lst)
print(list(res))  # 输出 [1, 4, 9, 16, 25]

# 案例2：两个列表元素对应相加
lst1 = [10, 20, 30]
lst2 = [1, 2, 3]
res2 = map(lambda x, y: x + y, lst1, lst2)
print(list(res2))  # 输出 [11, 22, 33]
```

##### 3.3.2 filter() 筛选函数

功能：通过函数逻辑**过滤可迭代对象**，保留函数返回True的元素，剔除False元素，返回迭代器。

语法：`filter(判断函数, 可迭代对象)`

```python
# 案例：筛选列表中的偶数
lst = [1, 2, 3, 4, 5, 6, 7, 8]
res = filter(lambda x: x % 2 == 0, lst)
print(list(res))  # 输出 [2, 4, 6, 8]
```

##### 3.3.3 sorted() 排序高阶函数

功能：全局排序函数，支持自定义排序规则、升降序，区别于列表自带sort()方法（原地排序），**sorted返回新列表，不修改原数据**。

语法：`sorted(可迭代对象, key=排序规则函数, reverse=是否倒序)`

```python
# 案例1：基础排序
lst = [5, 2, 9, 1, 5, 6]
print(sorted(lst))  # 升序
print(sorted(lst, reverse=True))  # 降序

# 案例2：自定义规则：按绝对值排序
lst2 = [-5, -2, 9, -1, 6]
res = sorted(lst2, key=lambda x: abs(x))
print(res)  # 输出 [-1, -2, -5, 6, 9]

# 案例3：字典列表按指定字段排序
user_list = [{"name": "张三", "age": 20}, {"name": "李四", "age": 18}, {"name": "王五", "age": 25}]
# 按年龄升序排序
res2 = sorted(user_list, key=lambda x: x["age"])
print(res2)
```

##### 3.3.4 reduce() 累积运算函数

功能：对可迭代对象元素**依次累积运算**，先计算前两个元素，结果与下一个元素继续运算，最终返回**单个结果值**。

注意：reduce不属于内置全局函数，需要从functools模块导入。

语法：`reduce(累积函数, 可迭代对象, 初始值)`

```python
from functools import reduce

# 案例1：列表所有元素累加
lst = [1, 2, 3, 4, 5]
res = reduce(lambda x, y: x + y, lst)
print(res)  # 输出 15

# 案例2：带初始值累积
res2 = reduce(lambda x, y: x * y, lst, 10)
print(res2)  # 10*1*2*3*4*5 = 1200
```

#### 3.4 四大内置高阶函数核心区别

- **map**：一对一映射，元素数量不变，批量处理元素；
- **filter**：条件筛选，元素数量减少或不变，过滤数据；
- **sorted**：自定义排序，元素数量不变，重塑数据顺序；
- **reduce**：累积计算，最终返回一个值，聚合统计数据。

---
> 🏠 **[返回主页 README](./README.md)** \| ◀️ **上一篇：[05. Python 文件流总结](./05Python%E6%96%87%E4%BB%B6%E6%B5%81%E6%80%BB%E7%BB%93.md)** \| ▶️ **下一篇：[07. Python OOP+模块+异常](./07Python%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%2B%E6%A8%A1%E5%9D%97%2B%E5%BC%82%E5%B8%B8%E6%80%BB%E7%BB%93.md)** \| 🎓 **[进入本模块面试高频题](./interview/01_云原生与Python高频面试题.md)**
